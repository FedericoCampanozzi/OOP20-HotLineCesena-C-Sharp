namespace Zammarchi.Items
{
    /// <summary>
    ///     Those items that can only be picked up, not collected.
    /// </summary>
    public enum ItemsType
    {
        BriefCase,
        AmmoBag
    }
}
